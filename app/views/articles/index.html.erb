<h1>Articles</h1>

<% if current_user%>
  <% if current_user.admin? %>
    Our blog has <%= Article.public_count %> articles and counting!
  <% end %>
<% end %>

<ul>
  <% @articles.each do |article| %>
    <% unless article.archived? %>
      <li>
        <%= link_to article.title, article %>
      </li>
    <% end %>
  <% end %>
</ul>

<% if user_signed_in? %>
  <%= link_to "New Article", new_article_path %> <span>(for users only)</span>
  <% else %>
  <p><s>New Article</s> (for users only)</p>
<% end %>

